web:
  build: .
  dockerfile: RTM_DOFILE
  ports:
   - "RTM_PORT:8087"
  links:
   - db
   - db_client
db_client: echo "create database resttest" | mysql -u root -p -h RTM_DB_HOST
  image: mysql:5.6
  command:
  links:
   - db
db:
  image: mysql:5.6
  environment:
     - MYSQL_ALLOW_EMPTY_PASSWORD=true
